version: '3'
services:
  db:
    image: microsoft/mssql-server-linux
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=password123!
      - MSSQL_PID=Express 
    ports:
      - 1433:1433
    volumes:
      - sqlvolume:/var/opt/mssql
volumes:
  sqlvolume:
